// import { render, screen } from '@testing-library/react';  // Importing testing utilities from React Testing Library
// import App from './App';  // Importing the App component that we want to test

// // Define a test block for rendering and checking the App component
// test('renders learn react link', () => {
//   // Render the App component in a virtual DOM provided by the testing library
//   render(<App />);

//   // Find an element on the screen that contains the text 'learn react' (case-insensitive)
//   const linkElement = screen.getByText(/learn react/i);

//   // Assert that the element is found and present in the document (i.e., the virtual DOM)
//   expect(linkElement).toBeInTheDocument();
// });
